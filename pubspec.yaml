name: nails_app
description: Catálogo para manicuristas
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.2
  path: ^1.9.0
  intl: ^0.19.0
  google_fonts: ^8.1.0

flutter:
  uses-material-design: true